
export enum Status {
    available = "Available",
    booked = "Booked",
    check_in = "Check In",
    check_out = "Check Out",
    in_progress = "In Progress",
    active = "Active",
    inactive = "Inactive"
}

export enum StatusSlice {
    idle = "idle",
    pending = "pending",
    rejected = "rejected",
    fulfilled = "fulfilled"
}